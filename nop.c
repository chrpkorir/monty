#include "monty.h"

/**
 * nop - Does nothing
 * @stack: doesn't matter
 * @line_cnt : for nothing
 *
 * Return: NOTHING
 */
void nop(stack_t **stack, unsigned int line_cnt)
{
	(void) stack;
	(void) line_cnt;
}
